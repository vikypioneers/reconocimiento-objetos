services:
  - type: web
    name: reconocimiento-objetos
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 1 --timeout 120 --access-logfile - app:app
    healthCheckPath: /health
    autoDeploy: true
